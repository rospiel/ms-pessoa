server:
  port: 7171

spring:
  datasource:
    driver-class-name: org.h2.Driver
    # platform: mysql
    url: jdbc:h2:mem:testdb
    username: sa
    password:

  h2:
    console:
      enabled: true
      path: /h2-console
